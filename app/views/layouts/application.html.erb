<!DOCTYPE html>
<html>
  <head>
    <title>Manage Pantory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arimo:ital,wght@0,400..700;1,400..700&family=Inter:wght@400;500;600;700&family=Outfit:wght@400;500;600;700&display=swap" rel="stylesheet">
  </head>

  <body>
    <div class="main-content">
      <%= render "shared/flash" %>

      <% unless controller_name == 'sessions' && action_name == 'new' && controller_path.start_with?('devise/') %>
        <header class="app-header">
          <h1 class="app-title"><%= link_to t("views.common.app_name"), root_path, style: "text-decoration: none; color: inherit;" %></h1>
          <p class="app-subtitle"><%= t("views.items.index.subtitle") %></p>
          
          <% if user_signed_in? %>
            <div style="margin-top: 16px; display: flex; align-items: center; gap: 8px;">
              <% if current_user.image.present? %>
                <%= image_tag current_user.image, size: "28x28", style: "border-radius: 50%;" %>
              <% end %>
              <span style="font-size: 14px; color: var(--color-subtitle);"><%= current_user.name %></span>
              <%= link_to t("views.common.logout"), destroy_user_session_path, data: { turbo_method: :delete }, style: "font-size: 14px; color: var(--color-footer-text);" %>
            </div>
          <% end %>
        </header>
      <% end %>

      <main>
        <%= yield %>
      </main>
    </div>
    <script type="module">
      import * as Turbo from "https://cdn.jsdelivr.net/npm/@hotwired/turbo@8.0.4/dist/turbo.es2017-esm.js"
    </script>
  </body>
</html>