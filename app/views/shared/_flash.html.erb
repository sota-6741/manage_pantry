<div id="flash">
  <% flash.each do |type, message| %>
    <div style="padding: 10px; margin-bottom: 10px; border-radius: 5px; <%= type == 'notice' ? 'background-color: #d1fae5; color: #065f46;' : 'background-color: #fee2e2; color: #991b1b;' %>">
      <%= message %>
    </div>
  <% end %>
</div>
