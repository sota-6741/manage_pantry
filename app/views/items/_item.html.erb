<div id="<%= dom_id(item) %>">
  <div>

    <%# アイテム名 %>
    <div>
      <h3><%= link_to item.name, item_path(item) %></h3>
    </div>

    <%# 詳細情報 %>
    <div>
      <%# カテゴリー %>
      <div>
        <span>
          <%= item.category&.name || "未分類" %>
        </span>
      </div>

      <%# ストック調整フレーム (差し替え対象) %>
      <%= turbo_frame_tag dom_id(item, :adjustment) do %>
        <%= render "stock_adjustments/display", item: item %>
      <% end %>

      <%# 賞味期限 %>
      <div>
        <span><%= format_date(item.expiration_date) %></span>
      </div>
    </div>
  </div>
</div>